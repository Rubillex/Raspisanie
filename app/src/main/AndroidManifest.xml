<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.prokrostinatorbl.raspisanie">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.android.vending.BILLING" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_book"
        android:label="@string/app_name"
        android:roundIcon="@drawable/card"
        android:supportsRtl="true"
        android:theme="@style/Light">
        <activity android:name=".donate"></activity>
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".auth" />
        <activity android:name=".app_info" />
        <activity android:name=".FUCKTABLE" />
        <activity android:name=".Setting" />
        <activity android:name=".institut" />
        <activity android:name=".group_list" />
        <activity android:name=".maps" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>